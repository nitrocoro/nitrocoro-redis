# nitrocoro-redis

Redis extension for NitroCoro.

## Requirements

- NitroCoro
- CMake 3.15+
- C++20 compiler

## Build

```bash
mkdir build && cd build
cmake -DNITROCORO_BUILD_DIR=../../nitrocoro/build -DNITROCORO_SOURCE_DIR=../../nitrocoro ..
make
```
